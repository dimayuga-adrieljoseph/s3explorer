services:
  # Local MinIO for development
  minio:
    image: minio/minio:latest
    container_name: s3explorer-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin123}
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  app:
    build: .
    container_name: s3explorer
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATA_DIR=/data
      # Strong password, or skip and configure via setup wizard on first launch
      - APP_PASSWORD=${APP_PASSWORD}
      # Session signing key (use: openssl rand -hex 32)
      - SESSION_SECRET=${SESSION_SECRET}
    volumes:
      - app_data:/data
    depends_on:
      - minio

volumes:
  minio_data:
  app_data:
# Usage:
# 1. Create .env file with:
#    APP_PASSWORD=YourSecureP@ssw0rd!
#    SESSION_SECRET=$(openssl rand -hex 32)
#    MINIO_ROOT_USER=minioadmin
#    MINIO_ROOT_PASSWORD=minioadmin123
#
# 2. docker-compose up -d
